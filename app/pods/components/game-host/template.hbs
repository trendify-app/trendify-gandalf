{{!-- {{#paper-toolbar}}
  {{#paper-toolbar-tools}}
    {{#paper-button iconButton=true}}
        {{paper-icon "menu"}}
    {{/paper-button}}
    <h2>
      Trendify 
    </h2>
    <span class="flex"></span>
    {{#paper-menu as |menu|}}
        {{#menu.trigger}}
            {{#paper-button iconButton=true}}
                {{paper-icon "more_vert"}}
            {{/paper-button}}
        {{/menu.trigger}}
        {{#menu.content width=2 as |content|}}
            {{#each items as |item|}}
            {{#content.menu-item onClick=(action "moreActions" item)}}
                {{paper-icon item.icon}} {{item.title}}
            {{/content.menu-item}}
            {{/each}}
        {{/menu.content}}
    {{/paper-menu}}
  {{/paper-toolbar-tools}}
{{/paper-toolbar}} --}}

<h2 class="game-header">
    Trendify 
</h2>
<div class="game-card">
    <div class=""></div>
</div>

{{yield}}