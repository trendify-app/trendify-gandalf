{{#if (eq name 'host')}}
  {{game-host startGame=(action 'game_start')
    users=users
    gameState=gameState
    trendData=trendData
    challenge=challenge
    rounds=rounds
    name=name
    room=room
  }}
{{/if}}
{{yield}}
